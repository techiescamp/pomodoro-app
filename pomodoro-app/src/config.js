const config = {
    development: {
        // apiUrl: process.env.REACT_APP_API_URL
        apiUrl: 'http://localhost:7000'
    },
    production: {
        apiUrl: ''
    }
}

export default config;